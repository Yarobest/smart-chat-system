import { ActivityIndicator, Pressable, Text } from 'react-native';
type Props={label:string;loading?:boolean;disabled?:boolean;onPress:()=>void;variant?:'primary'|'outline'|'danger';className?:string};
export function AsyncButton({label,loading=false,disabled=false,onPress,variant='primary',className=''}:Props){const blocked=loading||disabled;const tone=variant==='danger'?'border border-red-200 bg-red-50':variant==='outline'?'border border-blue-600 bg-white':'border border-blue-600 bg-blue-600';const text=variant==='danger'?'text-red-700':variant==='outline'?'text-blue-700':'text-white';return <Pressable disabled={blocked} onPress={onPress} className={`min-h-12 flex-row items-center justify-center rounded-lg px-4 py-3 ${tone} ${blocked?'opacity-50':''} ${className}`}>{loading?<ActivityIndicator size="small" color={variant==='primary'?'white':'#2563EB'}/>:null}<Text className={`${loading?'ml-2':''} text-base font-bold ${text}`}>{loading?'Please wait...':label}</Text></Pressable>}
